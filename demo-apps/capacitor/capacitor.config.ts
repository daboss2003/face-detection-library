import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.liveness.demo.capacitor",
  appName: "Liveness Capacitor Demo",
  webDir: "dist",
};

export default config;
